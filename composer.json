{
    "name": "voxx/events",
    "description": "Broadcast php generated Server-Sent-Events(SSE) to multiple concurrently connected clients leveraging custom NodeJS EventManager.",

    "require": {
        "php": ">=5.6"
    },
    "autoload": {
        "psr-4": {
            "voxx\\Events\\": "src/"
        }
    },
    "minimum-stability": "dev",
    "prefer-stable": true
}
